# Techages AI — FastAPI AI/voice microservice (lean, local-first).
# Core API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.0
python-dotenv>=1.0

# DB (async Postgres + pgvector)
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pgvector>=0.3

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0,<5.0

# LLM access via LangChain (provider-swappable: Ollama today, OpenAI/Anthropic later)
langchain-ollama>=1.0
langchain-core>=1.0
httpx>=0.27          # health ping only
pyyaml>=6.0          # editable YAML prompts (app/prompts/*.yaml)

# Tests
pytest>=8.0
pytest-asyncio>=0.23

# --- Local female TTS (Kokoro). Install the model files into backend/voices/:
#   pip install kokoro-onnx soundfile
#   curl -L -o voices/kokoro-v1.0.onnx <release>/kokoro-v1.0.onnx
#   curl -L -o voices/voices-v1.0.bin  <release>/voices-v1.0.bin
# Without them, /voice falls back to the macOS `say` voice.
kokoro-onnx>=0.4
soundfile>=0.12
