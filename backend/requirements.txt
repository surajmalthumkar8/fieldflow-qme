# Techages AI — FastAPI AI/voice microservice (lean, local-first).
# Core API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.0
python-dotenv>=1.0

# DB (async Postgres + pgvector)
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pgvector>=0.3

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0,<5.0

# Ollama + integrations via plain HTTP (no heavyweight LLM frameworks)
httpx>=0.27

# Tests
pytest>=8.0
pytest-asyncio>=0.23

# --- Optional: local female TTS upgrade (Kokoro). Install separately when ready:
#   pip install kokoro-onnx soundfile
# Until then the /voice endpoint falls back to the macOS `say` voice (Samantha).
